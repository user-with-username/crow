[package]
name = "project_with_external_deps"
version = "0.1.0"
standard = "20"

[target."cfg(windows)".dependencies]
formatter = { path = "../example_lib" }
winapi = { system = true, libs = ["ws2_32", "user32", "gdi32", "ole32", "oleaut32", "shell32", "advapi32", "dbghelp"] }
