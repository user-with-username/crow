[package]
name = "my_project"
version = "0.1.0"
standard = "20"

[dependencies]
formatter = { path = "../formatter" }

[build]
hooks = [
    "echo Running pre-build hook",
    "echo This is a placeholder for actual build commands"
]